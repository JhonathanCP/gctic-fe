import React from 'react';
import {PanelComponent} from '../components/PanelComponent';  // Asegúrate de proporcionar la ruta correcta al componente de autenticación

export function PanelPage() {
  return (
    <div>
      <PanelComponent />
    </div>
  );
};