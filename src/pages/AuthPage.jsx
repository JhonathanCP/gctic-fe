import React from 'react';
import {AuthComponent} from '../components/AuthComponent';  // Asegúrate de proporcionar la ruta correcta al componente de autenticación

export function AuthPage() {
  return (
    <div>
      <AuthComponent />
    </div>
  );
};