          <div>
      <h1>Grupos y Reportes</h1>
      {grupos.map((grupo) => (
        <div key={grupo.id}>
          <h2>{grupo.nombre}</h2>
          <p>{grupo.descripcion}</p>
          <ul>
            {grupo.reportes.map((reporte) => (
              <li key={reporte.id}>
                <h3>{reporte.nombre}</h3>
                <p>{reporte.descripcion}</p>
                <a href={reporte.link} target="_blank" rel="noopener noreferrer">
                  Ver Reporte
                </a>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>